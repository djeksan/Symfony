
<html>
<body>
<head></head>

{% block body %}
    <a href="{{ path('admin_prod_list') }}">To product</a>
<h2> Foto from Product </h2> <h1> {{product.title }}</h1>

    <table>
{% if product.id==true  %}

        {% for photo in product.photos %}

            <a href="/photos/{{photo.getFilename()}}" />
        <img src="/photos/{{photo.getSmalfileName()}}" />

            {{ product.title }}
        {% endfor %}

    </table>

    {% else %}

<img src="/{{ asset('photos/noImg.gif') }}"  />

{% endif %}

{% endblock %}

</body>
</html>